# Other weird behavor: two docs folders

When I render with `bookdown::render_book("index.Rmd")` a `docs` folder is created. When I render again, a *second* `docs` folder is created within the first one:

[`docs/docs`](docs/)
